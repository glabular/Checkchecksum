<Application x:Class="ChecksumCalculatorWpf.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ChecksumCalculatorWpf">
    <Application.Resources>
        <FontFamily x:Key="PoppinsFont">pack://application:,,,/Resources/Fonts/#Poppins</FontFamily>

        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="FontFamily" Value="{StaticResource PoppinsFont}" />
        </Style>        
        
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="FontFamily" Value="{StaticResource PoppinsFont}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        
        <Style TargetType="{x:Type Button}">
            <Setter Property="FontFamily" Value="{StaticResource PoppinsFont}" />
        </Style>
    </Application.Resources>
</Application>
